# === CORE DEPENDENCIES ===
pywebview==6.2.1
Flask==3.1.3
# 0.136.3 carries a WITHDRAWN OSV malware flag (MAL-2026-4750, false positive)
# that some scanners still report — stay on a clean release.
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.4
pydantic==2.13.4
requests==2.34.2
python-multipart==0.0.32
psutil==7.2.2

# === STREAMING & INTEGRATION ===
# NOTE: 7.0.0 exists but is a fresh major (Aug 2026) — do not bump without
# testing against a real livestream; the bridge depends on 6.x event APIs.
TikTokLive==6.6.6
mcrcon==0.7.0

# === BUILD & UTILS ===
pyinstaller==6.22.2
packaging==26.3
ruamel.yaml==0.19.1
filelock==3.32.3

# === SECURITY ===
# Fernet/PBKDF2HMAC only (secure_storage) — stable APIs across 44.x-50.x.
cryptography==50.0.0

# === PYWEBVIEW QT BACKEND ===
PyQt6==6.11.0
PyQt6-WebEngine==6.11.0
qtpy==2.4.3

# === TESTING ===
PyYAML==6.0.3
pytest==9.1.1
pytest-timeout==2.4.0
pytest-asyncio==1.4.0
