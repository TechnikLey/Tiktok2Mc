[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    integration: Integration tests that hit API endpoints
    unit: Pure unit tests with no external dependencies
    validator: Validator (.mca file format) tests
